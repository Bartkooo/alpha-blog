<%= render 'shared/errors', obj: @user %>

<%= form_with(model: @user, local: true) do |u| %>
  <div class="container border rounded p-4 bg-light shadow" style="width: 45em;">
    <div class="mb-3">
      <%= u.label :username, class:"form-label" %></br>
      <%= u.text_field :username, class:"form-control" %>
    </div>
    <div class="mb-4">
      <%= u.label :email, class:"form-label" %></br>
      <%= u.email_field :email, class:"form-control" %>
    </div>
    <div class="mb-4">
      <%= u.label :password, class:"form-label" %></br>
      <%= u.password_field :password, class:"form-control" %>
    </div>
    <%= u.submit "Sign up", class:"btn btn-success" %>
    <%= link_to "Cancel", articles_path, class:"btn btn-danger" %>
  </div>
<% end %>
